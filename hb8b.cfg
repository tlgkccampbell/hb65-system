MEMORY {
       ZP: start = $0000, size = $0080, type = rw, define = no;
REGISTERS: start = $0080, size = $0080, type = rw, define = yes;
    STACK: start = $0100, size = $0100, type = rw, define = yes;
     WRAM: start = $0200, size = $1E00, type = rw, define = yes;
     SROM: start = $8000, size = $8000, type = ro, define = yes, fill = yes, file = "hb8b-wozmon.bin";
}

SEGMENTS {
    ZEROPAGE: load =   ZP, type = zp, define = yes;
    DATA:     load = WRAM, type = rw;
    CODE:     load = SROM, type = ro;
    RODATA:   load = SROM, type = ro;
    WOZMON:   load = SROM, type = ro, offset = $7E00;
    VECTORS:  load = SROM, type = ro, offset = $7FFA;
}